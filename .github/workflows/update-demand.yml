name: Update Demand Data

on:
  schedule:
    - cron: '0 2 * * 1'
  workflow_dispatch:

jobs:
  update-demand:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install pytrends pandas requests --break-system-packages

      - name: Run demand patcher
        run: python patch_demand.py

      - name: Commit and push demand.json
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/demand.json
          git diff --staged --quiet || git commit -m "chore: update demand scores [skip ci]"
          git push
